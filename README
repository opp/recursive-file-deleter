Recursive file deleter

Originally created to delete the annoying .DS_Store files on MacOS. This is why the default file this searches for is the .DS_Store file.

For now, this only supports basic searching. Next up is wildcard searching and more user-friendly options.